/** @type {import('next').NextConfig} */
const nextConfig = {
  // Single self-contained container for Coolify (handoff §8 / unit eng-coolify-singleimage).
  output: 'standalone',
  reactStrictMode: true,
};

export default nextConfig;
